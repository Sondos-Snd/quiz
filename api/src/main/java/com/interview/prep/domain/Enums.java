package com.interview.prep.domain;

public final class Enums {
    private Enums() {}

    public enum Profession { avocat, notaire, huissier }
    public enum ExpertStatus { pending, verified, suspended }
    public enum Channel { video, phone, chat }
    public enum Sender { user, expert }
}
